Let's implement functionality to create a `Person`. We can inject the `PersonDataService` into a `PersonController` to save a person.
Upon a successful save, log a message. On an unsuccessful save, catch the exception and log an error message.

[source, groovy]
.grails-app/controllers/demo/PersonController.groovy
----
include::{sourceDir}/grails-app/controllers/demo/PersonController.groovy[tag=personControllerCreate]
include::{sourceDir}/grails-app/controllers/demo/PersonController.groovy[tag=personControllerEnd]
----